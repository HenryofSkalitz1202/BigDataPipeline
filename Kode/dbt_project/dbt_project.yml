name: 'tpch_transformations'
version: '1.0.0'
config-version: 2

profile: 'trino_lakehouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  tpch_transformations:
    marts:
      +materialized: table